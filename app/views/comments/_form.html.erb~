<%= form_for [@post, @comment] do |f| %>
   <%= f.label :description %>
   <%= f.text_area :description %>
   <br/>
   <%= f.submit "Create"%>
  <% end %>
       <ul>
  <% @post.comments.each do |comment| %>
    <li>
    <%= comment.description %>
    </li>
  <% end %>
</ul>